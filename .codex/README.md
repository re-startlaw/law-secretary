# Codex Local Helpers

This directory is for repository-local Codex helper files.

`shell_path_utf8.txt` is ignored by Git and may be used as a UTF-8 path handoff
file for scripts that should not receive Japanese paths directly on the shell
command line.
